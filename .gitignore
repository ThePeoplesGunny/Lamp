# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/

# Node
node_modules/
frontend/dist/

# Data
backend/data/external/
backend/data/graphs/
backend/data/verses/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local

# Per-machine session handoff state, written by the SessionEnd hook.
# Machine-local: records HEAD, dirty files and a timestamp, so it churns every
# session and would conflict across machines. The hook reads it from disk, not git.
.claude/session_handoff.json
